<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width,initial-scale=1' />
    <title>Password Reset — NDU TV</title>
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');
      :root {
        color-scheme: light;
      }
      body {
        margin: 0;
        padding: 0;
        background: #f4f6f8;
        font-family:
          'Inter',
          system-ui,
          -apple-system,
          Segoe UI,
          Roboto,
          Helvetica,
          Arial,
          sans-serif;
        color: #111;
      }
      .wrap {
        max-width: 600px;
        margin: 28px auto;
        padding: 20px;
      }
      .card {
        background: #fff;
        border-radius: 12px;
        padding: 28px;
        box-shadow: 0 6px 18px rgba(18, 26, 38, 0.06);
      }
      .logo {
        display: block;
        margin: 0 auto 18px;
        height: 52px;
        object-fit: contain;
      }
      h1 {
        font-size: 20px;
        margin: 0 0 12px;
        font-weight: 700;
        color: #0f1724;
        text-align: left;
      }
      p {
        margin: 0 0 14px;
        color: #55606a;
        font-size: 14px;
        line-height: 1.5;
      }
      .cta {
        display: block;
        text-align: center;
        margin: 18px 0;
      }
      .btn {
        display: inline-block;
        padding: 12px 20px;
        border-radius: 8px;
        background: #0066ff;
        color: #fff;
        text-decoration: none;
        font-weight: 600;
      }
      .code-box {
        background: #f1f5ff;
        border-radius: 10px;
        padding: 18px;
        text-align: center;
        margin: 16px 0;
      }
      .code {
        font-size: 24px;
        font-weight: 700;
        letter-spacing: 4px;
      }
      .small {
        font-size: 13px;
        color: #7a8794;
      }
      .muted {
        color: #9aa3ae;
        font-size: 12px;
        text-align: center;
        margin-top: 18px;
      }
      .footer {
        font-size: 12px;
        color: #9aa3ae;
        text-align: center;
        margin-top: 20px;
      }
      a.link {
        color: #0066ff;
        word-break: break-word;
      }
      @media (max-width: 420px) {
        .wrap {
          padding: 12px;
        }
        .card {
          padding: 18px;
        }
      }
    </style>
  </head>
  <body>
    <div class='wrap'>
      <div class='card' role='article' aria-label='Password reset email'>
        <img
          src='https://res.cloudinary.com/dymdpsbvl/image/upload/v1759585053/IMG_5535_a81reu.png'
          alt='NDU TV'
          class='logo'
        />
        <h1>Password reset request</h1>
        <p>Hi {{name}},</p>
        <p>We received a request to reset the password for your NDU TV account.
          Use the button below to choose a new password. This link/code will
          expire in
          <strong>{{expires_in}}</strong>.</p>

        <!-- If using one-time code -->
        {{#if code}}
          <div class='code-box' aria-hidden='false'>
            <div class='small'>Your verification code</div>
            <div class='code' aria-label='verification code'>{{code}}</div>
            <div class='small' style='margin-top:8px;'>Expires in
              {{expires_in}}</div>
          </div>
        {{/if}}

        <!-- Primary action button (link fallback shown below) -->
        {{#if action_url}}
          <div class='cta'>
            <a
              href='{{action_url}}'
              class='btn'
              target='_blank'
              rel='noopener'
            >Reset password</a>
          </div>
          <p class='small'>Or paste this link into your browser:</p>
          <p class='small'><a
              href='{{action_url}}'
              class='link'
              target='_blank'
              rel='noopener'
            >{{action_url}}</a></p>
        {{/if}}

        <p class='small'>If you didn't request a password reset, you can safely
          ignore this email — no changes will be made.</p>

        {{!-- <p class='muted'>Need help? Contact
          <a
            href='mailto:{{support_email}}'
            class='link'
          >{{support_email}}</a></p> --}}
        <div class='footer'>© {{year}} NDU TV. All rights reserved.</div>
      </div>
    </div>
  </body>
</html>